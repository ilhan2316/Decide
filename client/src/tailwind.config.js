/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",  // No need for client/ prefix since we're in the client folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}